# Scytale-exercise

To run the whole project simply run:
`docker-compose up`. it will start the database and after that the application.

If you want to take the whole thing down run:
`docker-compose down --volumes` - it will reset the database and make sure that next time your project will start from scratch.